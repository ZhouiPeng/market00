<?xml version="1.0" encoding="UTF-8" ?>
    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.otsuland.market.mapper.SessionMapper">

    <select id="selectBySidAndRid">
        select * from session
        where (a_id = #{rid} and b_id = #{sid})
            or (a_id = #{sid} and b_id = #{rid});
    </select>

    <!-- selectBySidOrRid --> 

    <select id="selectBySidOrRid">
        select * from session
        where a_id = #{sid} or b_id = #{sid};
    </select>
</mapper>